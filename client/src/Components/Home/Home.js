import './Home.css'
import Background from './background.jpg';

const Home = () => {
    return(
        <div data-testid='home' className='home' >
            
        </div>
    );
};

// <img src={Background} />

export default Home;